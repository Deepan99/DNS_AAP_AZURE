---
# Azure dynamic inventory. The AAP Azure Resource Manager credential supplies
# authentication; no cloud credentials are stored in Git.
plugin: azure.azcollection.azure_rm
include_vm_resource_groups:
  - RG-HUB-EASTUS
  - RG-SPOKE-EASTUS

auth_source: auto

groups:
  dns_servers: "tags.Role | default('') == 'dns_server'"
  dns_clients: "tags.Role | default('') == 'dns_client'"

keyed_groups:
  - key: tags.Role
    prefix: role
  - key: resource_group
    prefix: rg

# Removed hostvar_expressions to avoid parsing errors with Azure inventory plugin
# SSH configuration will be handled via group_vars and job template settings
