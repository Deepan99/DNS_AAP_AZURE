---
# ==============================================================================
# Playbook: Deploy ISC BIND Private DNS Server
# Target: Bastion Host / DNS Server Group
# AAP / AWX Entry Point
# ==============================================================================

- name: Deploy and configure BIND DNS servers
  hosts: dns_servers
  become: true
  gather_facts: true
  roles:
    - role: bind_dns
